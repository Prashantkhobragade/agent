generate_json_task:
  description: >
    Process the provided input data and generate a structured JSON output. Ensure that the output contains all necessary information and is formatted correctly.
  expected_output: >
    A JSON object containing the processed data with the following structure:
    {
      "name": "string",
      "age": "integer",
      "email": "string"
    }
